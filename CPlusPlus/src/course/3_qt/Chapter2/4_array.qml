import QtQuick 2.7
import QtQuick.Controls 2.0


ApplicationWindow
{
    visible: true;
    width:800;
    height: 600;

    Component.onCompleted: {
        var a = new Array();
        var b = new Array(10);
        var c = new Array(10,6,3,21,22,30,8);

    }
}
